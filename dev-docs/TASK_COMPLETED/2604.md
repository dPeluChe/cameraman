# Tareas Completadas — Abril 2026

> Periodo: 2026-04-01 al 2026-04-02
> Per-segment editing full-stack, camera border, telemetry recording, autosave, auto-zoom rendering.

---

## Sesion 6 (2026-04-01): Segment Refactor + Compositor

### Segment Model + Split Fix
- [x] `Segment.cameraPosition: CameraPosition?` — per-segment camera override con decoder backward-compatible
- [x] Split bug fix — `takeId`, `zoom`, y `cameraPosition` se propagan a ambos segmentos al hacer split
- [x] 6 unit tests: propagation de takeId, zoom, cameraPosition, speed + backward decoding + default nil

### Per-Segment Speed UI
- [x] `ProjectEditor.updateSegmentSpeed()` con clamp 0.25x-4x
- [x] Speed badge naranja visible en segmentos con velocidad != 1x
- [x] `SegmentInspectorBar` (nuevo) — picker con presets (0.25x-4x)

### Per-Segment Camera UI
- [x] `ProjectEditor.updateSegmentCameraPosition()` — set custom override o nil para revert
- [x] Camera override indicator verde en segmentos con posicion custom

### Media Item Drag
- [x] DragGesture con feedback visual en vivo + commit en onEnded
- [x] Labels mejorados con icono waveform/photo + tooltip de duracion

### Compositor — Visual Effects Rendering
- [x] Background rendering: solid (CIColor), gradient (CILinearGradient), blur (CIGaussianBlur)
- [x] Padding: scale down + translate center
- [x] Corner radius: CIBlendWithMask con rounded rect path
- [x] Per-segment instructions en PreviewComposition
- [x] `cameraTransform()` extraido como helper estatico reutilizable

### Audio Drift Detection
- [x] `AudioDriftDetector.detect()` — compara duraciones video vs system/mic audio
- [x] `DriftReport` con drift en ms y flag `hasSignificantDrift` (>100ms)

---

## Sesion 7 (2026-04-02): Full-Stack Per-Segment + Auto-Zoom

### Camera Border en PiP
- [x] `CameraPosition.borderWidth` (0-8px) + `borderColor` (hex) con decoder backward-compatible
- [x] Compositor: `renderCameraBorder()` via CGPath stroke (circle/roundedRect/capsule/rectangle)
- [x] Border cache: evita CGContext allocation per frame (~240MB/s savings en 1080p)
- [x] UI: slider + 10 color presets en PiPConfigurationView
- [x] `PiPLayoutHelper` preserva border al move/resize/preset

### Per-Segment Camera — Funcional End-to-End
- [x] Auto-create override al drag (sin boton "Custom" extra)
- [x] `PiPConfigurationView` + `PiPCanvasEditor` segment-aware con routing unificado `updateCamera()`
- [x] `selectedSegmentId` promovido a `ProjectEditorViewModel` (shared timeline ↔ sidebar)
- [x] Indicador visual: naranja "drag to customize" / verde "custom"
- [x] Instrucciones contiguas en compositor (prev.end = next.start) — fix de video negro en splits
- [x] Export usa per-segment instructions con visual effects

### Per-Segment Audio
- [x] `Segment.volume: Double?` (0-3x) + `Segment.audioMuted: Bool?` en modelo
- [x] `AudioMixBuilder.applySegmentVolumes()` con `setVolumeRamp` per-segment
- [x] `lastAudioMuteState` preservado en PreviewEngine (no se pierde al rebuild)
- [x] UI: mute button + volume slider + reset en SegmentInspectorBar
- [x] Split propaga volume/audioMuted

### Telemetry Recording
- [x] `TelemetryRecorder` integrado en `Recorder.startRecording()` / `stopRecording()`
- [x] `RecordingConfiguration.captureTelemetry = true` default
- [x] `moveRecordingFiles()` copia cursor.jsonl al proyecto

### Autosave
- [x] `ProjectEditor.scheduleAutosave()` con 1s debounce
- [x] Llamado desde `mutateSegment()`, `updateCameraPosition()`, `updatePublishedProject()`
- [x] Persiste via `ProjectLibrary.shared.updateProject()`

### Auto-Zoom Rendering
- [x] `MaskedVideoCompositor.activeZoomPlan` (static) — zoom plan accesible al compositor
- [x] Zoom transform per-frame: scale around focus point con Y-flip para CIImage coords
- [x] Auto-show suggestions al abrir proyecto con telemetry
- [x] Auto-apply zoom plan al generar suggestions
- [x] Threshold tuning: minClicksPerWindow 2→1, minMovementDistance 50→20px, DwellDetector mas sensible

### Code Quality (Simplify)
- [x] `mutateSegment()` generico reemplaza 4x boilerplate (speed/volume/mute/camera)
- [x] Missing undo en updateSegmentVolume/AudioMuted — ahora todos registran undo
- [x] Border cache, `cgColor(from:)` reutiliza `ciColor(from:)`, TOCTOU fix

### Deprecation + Compatibility Fixes
- [x] 11 `onChange(of:perform:)` migrados a macOS 14+ API
- [x] TimelineView body split en sub-views (fix type-checker timeout)
- [x] TeleprompterWindow concurrency fix
- [x] BackgroundControlsView+Helpers: `.gradient` case
- [x] `importantClicks` → `importantClickCount` argument label fix
