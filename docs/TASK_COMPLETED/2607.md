# Completed — July 2026

> Work finished during the synthetic-cursor / AI-stability push (branches around `fix/cursor-rendering` and `fix/local-ai-provider-tests`).

## LocalAIProviderTests crash fixes

- `CIRandomGenerator` in `LocalAIProviderHelpers.swift` was incorrectly trying to set `kCIInputImageKey`, causing a `NSUnknownKeyException`. Fixed by cropping the `CIRandomGenerator` output directly.
- `testGenerateBackgroundThumbnailGeneration`: corrected JPEG header comparison.
- `testBackgroundGenerationPerformance`: `applyGradientStyle` and `applyMinimalStyle` were returning uncropped `CIImage`s, leading to rendering failures and test timeouts. Fixed by cropping to the requested size.

## Synthetic cursor rendering

- `CursorPlan` (resampled path + click marks) and `CursorPlanGenerator` (normalizes raw telemetry into capture-local space) added in `Sources/EngineKit/Zoom/CursorPlan.swift`.
- `CursorRenderer.swift` added as a `MaskedVideoCompositor` extension: draws a configurable cursor dot and expanding click ripples on top of the composited frame.
- `MaskedVideoCompositionInstruction` extended with `cursorPlan` and `cursorConfig`; `MaskedVideoCompositor` calls `compositeCursor` after overlays.
- Preview pipeline (`PreviewComposition.swift` + `PreviewComposition+StaticClips.swift`) passes `PreviewEngine.cursorPlan` and `Project.syntheticCursor` through every compositor instruction, and enables the compositor when the cursor is enabled.
- Export pipeline (`VideoExportSession+Composition.swift`) gains the same wiring via new `ExportOptions.cursorPlan`; the compositor is forced on when the cursor is enabled.
- Added `CursorPlanTests.swift` and an `ExportOptions` cursor-plan test in `ExportEngineTests.swift`.
- Remaining: hide the real cursor during recording and add a Keycastr-style keystroke overlay from `keys.jsonl` (backlogged in `TASK_TODO.md`).

## Manual zoom keyframes + telemetry improvements (PR #42)

Branch: `feat/zoom-telemetry-improvements`

### Manual zoom
- `ZoomKeyframe` extended with `isManual` flag; `Project.manualZoomKeyframes` array added.
- `ZoomPlan+Merge.swift` merges auto + manual keyframes (manual precedence).
- `ZoomPlanGenerator.manualOnlyPlan(from:)` static helper for manual-only plan construction.
- `ProjectEditor` API: add / update / remove / clear / live-drag-timestamp / commit-drag with undo + autosave.
- Right panel `ManualZoomControlsView`: keyframe list, zoom/focus/easing editors, click-to-focus toggle, empty state.
- Preview overlay `ManualZoomFocusOverlay`: focus circle, selected highlight, crosshair cursor + orange tint when interactive.
- Timeline `ManualZoomKeyframeMarker`: draggable markers with zoom level labels (e.g. "2.0x"), tap-to-select (yellow highlight), right-click delete, live preview during drag (30fps throttle).
- Timeline `ZoomCurveOverlay`: mini graph of zoom intensity over time.
- `ZoomTypes.zoomLevel(at:)` / `focusPoint(at:)` fixed: return `defaultZoomLevel` / center before first keyframe (was returning first keyframe's value from t=0).
- `addManualZoomKeyframe` returns `UUID?` for auto-select of newly created keyframes.

### Auto-zoom default
- `FeatureFlags.autoZoom` defaults to `true` (was `false`).
- Dead `!FeatureFlags.autoZoom` branch removed from `TimelineView.zoomSuggestionButtons`.

### Telemetry
- `RecordingSession.errorCounts` dictionary tracks per-code errors (e.g. `-10877`).
- `CaptureEngine` logs `AVAssetWriter` error codes via `LogWarning`.

### Hide cursor
- `CaptureConfiguration.hideSystemCursor` property added.
- `CaptureSessionManager` sets `SCStreamConfiguration.showsCursor = !hideSystemCursor`.
- Recording UI gains "Hide Cursor" toggle (`cursorarrow.slash` / `cursorarrow` SF Symbols).

### Cleanup
- Removed 5 unused `@State` vars in `ManualZoomControlsView`.
- Removed dead `merged(withProject:)` method.
- Deduped manual-only plan construction via `manualOnlyPlan` helper.
- Added missing `Combine` import for `ClickToFocusState`.
